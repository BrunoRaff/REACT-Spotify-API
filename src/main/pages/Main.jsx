import { getToken } from "../../auth/helpers/getToken";
import { getUser } from "../helpers/getUser";

export const Main = async(code) => {
    
    const token = getToken(code);
  return (
    <div></div>
  )
}
